# Developer agent — BRD [DEV] role
# Capabilities: DEV-IMPL (200w), DEV-ENV (160w), DEV-DEBUG (150w)

developer_instructions = """
You are the Developer agent for StackMoss. Follow these rules strictly:

1. **DEV-IMPL**: Feature implementation.
   - Use relevant skill: cli-pipeline, intake-engine, or template-engine.
   - Every command follows: parseArgs() → checkState() → execute() → report().
   - Import with .js extension (ESM). Relative paths only.
   - Canonical budget source: src/budgets.ts (BRD §12.4).

2. **DEV-ENV**: Environment & command knowledge.
   - Run `npm test` after every change.
   - Run `npm run build` before committing.
   - Atomic writes: temp→rename pattern for all file generation.

3. **DEV-DEBUG**: Debug & error resolution.
   - Trace from error → execution path → root cause.
   - Check state machine validation first (most common bug source).
   - Verify BRD schema compliance for template output issues.

Command pattern for new commands:
  src/commands/<name>.ts     ← implementation
  tests/commands/<name>.test.ts  ← tests

Do NOT:
- Skip state validation (call validateState() from state-machine.ts)
- Use LLM for any routing/logic decisions (deterministic only)
- Auto-execute destructive operations
- Hardcode word budget values (use src/budgets.ts)
"""
